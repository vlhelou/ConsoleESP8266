<Page
	x:Class="uConsole.pgConexao"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:uConsole"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<StackPanel Orientation="Horizontal" Margin="50,10,0,0">
			<TextBlock Text="Conexão:"  />
			<ComboBox x:Name="cbSeriais" Margin="10,0,0,0"  >
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="{Binding Name}" />
						</StackPanel>
					</DataTemplate>
				</ComboBox.ItemTemplate>
				
			</ComboBox>
			<Button VerticalAlignment="Top" Content="refresh" Margin="10,0,0,0" Click="Refresh_Click"/>
		</StackPanel>
		
		<TextBlock Text="{Binding Diretorio}" Margin="0,80" />
	</Grid>
</Page>
